import { GoResult } from '@api3/promise-utils';
import { Gateway, Template } from './validation';

// TODO: https://api3dao.atlassian.net/browse/BEC-295
export const makeSignedDataGatewayRequest = async (_gateway: Gateway, _template: Template) => {
  return 123 as any as GoResult<any>;
};
